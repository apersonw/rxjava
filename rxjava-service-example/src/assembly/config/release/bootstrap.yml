spring:
  application:
    name: rxjava-service-${module.name}
  cloud:
    kubernetes:
      config:
        sources:
        - name: common-configmap
          namespace: default
  data:
    mongodb:
      database: rxjava-service-${module.name}