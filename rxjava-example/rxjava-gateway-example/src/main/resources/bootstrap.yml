server:
  port: 8080
spring:
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017
      database: rxjava-gateway-example
  cloud:
    kubernetes:
      config:
        enabled: false
      discovery:
        enabled: false
checktoken:
  service-id: localhost
  port: 8081